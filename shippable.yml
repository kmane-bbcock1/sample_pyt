language: python

# Version number
python:
  #- 2.6
  - 2.7
 # - 3.2
  #- 3.3
  #- 3.4
  #- 3.5
 # - pypy  
  #- ppy3

  
build:
 # pre_ci:
 #   - docker build -t=shippabledocker/pipelinebbtest .

  pre_ci_boot:
    image_name: drydock/u14pyt
    image_tag: prod
    pull: true
    
  ci:
    - python --version
  
  on_success:
    - echo "success"
    
  on_failure:
    - echo "failure"   
 
  post_ci:
    - docker build -t=shiphitchcock1/sample_python:$BRANCH.$BUILD_NUMBER .
    - docker push shiphitchcock1/sample_python:$BRANCH.$BUILD_NUMBER

integrations:
  hub:
    - integrationName: shiphitchcock1-hub 
      type: docker
