language: python

# Version number
python:
  #- 2.6
  #- 2.7
  #- 3.2
  #- 3.3
  #- 3.4
  #- 3.5
  #- pypy  
  - ppy3

services:
  - postgres
  
build:
 # pre_ci:
 #   - docker build -t=shippabledocker/pipelinebbtest .

  pre_ci_boot:
    image_name: drydock/u14pytall
    image_tag: prod
    pull: true
    
  ci:
    - python --version  
  
  on_success:
    - echo "success"
    
  on_failure:
    - echo "failure"
   # - ps -eax
   # - ls -al 

  post_ci:
    - docker build -t=shippabledocker/pipelinebbtest:$BRANCH.$BUILD_NUMBER .
    - docker push shippabledocker/pipelinebbtest:$BRANCH.$BUILD_NUMBER

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
